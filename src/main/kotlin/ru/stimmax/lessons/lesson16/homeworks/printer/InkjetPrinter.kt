package ru.stimmax.lessons.lesson16.homeworks.printer

import ru.stimmax.lessons.Background
import ru.stimmax.lessons.Colors

class InkjetPrinter : Printer() {

    private val colorPairs = listOf(
        Colors.RED to Background.WHITE,
        Colors.GREEN to Background.BLACK,
        Colors.YELLOW to Background.BLUE,
        Colors.BLUE to Background.YELLOW,
        Colors.PURPLE to Background.CYAN
    )


    override fun printText(text: String) {          //override — говорит компилятору, что мы переопределяем метод, который был объявлен в родительском классе (Printer).
        text.split(" ").forEachIndexed { index, word ->
            val (fg, bg) = colorPairs[index % colorPairs.size]  // выбираем циклически
            println("$fg$bg$word${Colors.RESET}")
        }
    }




//    .forEachIndexed { index, word -> ... }
//    forEachIndexed — это цикл по списку, но с индексом каждого элемента.

//    index — порядковый номер элемента в списке (0,1,2...)
//    word — сам элемент списка (в нашем случае слово из текста)


//    (fg, bg) — деструктуризация пары.
//    fg — цвет текста (foreground)
//    bg — цвет фона (background)


//    index % colorPairs.size — выбирает цвет циклически:
//    % — это оператор остатка от деления
//    index % colorPairs.size гарантирует, что индекс не выйдет за границы списка цветов.

//    Colors.RED to Background.WHITE — создаем пару (текст + фон)
//    forEachIndexed — позволяет использовать индекс, чтобы выбирать цвет циклично
//    ${Colors.RESET} — сброс, чтобы цвета не «переходили» на следующую строку


//    println("$fg$bg$word${Colors.RESET}")
//    println(...) — выводит текст в консоль и переходит на новую строку.
//    $fg — вставляет цвет текста (например, ANSI-код красного цвета)
//    $bg — вставляет цвет фона
//    $word — само слово из списка
//    ${Colors.RESET} — вставляет ANSI-код сброса цвета, чтобы следующий текст был обычным
//    Итог:
//    Каждое слово печатается в своём цвете, а потом цвета сбрасываются.

//    💡 Идея всей конструкции:
//    Разбиваем текст на слова
//    Проходим по каждому слову с индексом
//    Циклически выбираем пару цветов для слова
//    Выводим слово с этими цветами, затем сбрасываем стиль

}